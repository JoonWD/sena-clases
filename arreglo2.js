//este areglo se hace para que un usuario, almacene un numero y posteriormente este se vea en la consola.
function arreglo2() {
  var numero1 = Number(prompt("ingregrese un numero para verlo en la consola"));
  console.log(numero1);
  alert("el numero que digito es " + numero1);
}
